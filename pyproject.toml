[tool.ruff]
target-version = "py313"
line-length = 88
extend-exclude = [
    "*/migrations/*",
    "frontend/*",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    "E501",
]

[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "config.settings_test"
